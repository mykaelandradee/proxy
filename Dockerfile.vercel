# Vercel diagnostic container for MediaFlow Proxy
FROM mhdzumair/mediaflow-proxy:latest

ENV PATH="/mediaflow_proxy/.venv/bin:$PATH"

COPY diagnostic/server.py /diagnostic/server.py

EXPOSE 8888

ENTRYPOINT ["python", "/diagnostic/server.py"]
